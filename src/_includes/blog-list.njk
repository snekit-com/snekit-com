---
layout: base.njk
---
<div class="blog-posts">
  {% for post in collections.blog %}
    <article class="blog-post">
      <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
      <div class="post-meta">{{ post.date | formatDate }} | {{ post.data.author }}</div>
      <p>{{ post.data.description }}</p>
      <a href="{{ post.url }}" class="read-more">Read more</a>
    </article>
  {% endfor %}
</div>

<div class="contact-section">
  <p>Want to discuss a blog topic or suggest a new one? Reach out at <a href="mailto:admin@snek-it.com">admin@snek-it.com</a></p>
</div>